Before start, please make sure that computers run the server and client's program should be in the same local network and the one which run the server should knowing its IP address.
First, open the server's program (server.py ) to start a server.
Then, open client's program (Connectbox.py), type the server's IP address in to get connection with server and the game will be start automatically when 3 players are connected to the server.

